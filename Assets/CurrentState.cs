using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CurrentState : MonoBehaviour
{

    public Vector3 inkingPosePosition;
    public Quaternion inkingPoseRotation;
    public float tip_value;
    public float cluster_middle_value;
    public bool cluster_front_value;
    public bool cluster_back_value;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
